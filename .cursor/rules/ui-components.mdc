---
description: "USE WHEN: Building UI components, structuring layouts, and applying styles using Tailwind CSS."
globs: "src/components/**/*.{ts,tsx},src/features/**/components/**/*.{ts,tsx}"
alwaysApply: false
---

# UI Components

## Constraints
- **Hierarchy:** Organize by `ui` (stateless), `shared` (common), and `feature` (domain logic).
- **Atomic:** Keep `src/components/ui` pure; forbid API/Store dependencies.
- **Separation:** Separate data (container) from UI (presentation).
- **Composition:** Prefer children-based composition over large prop objects.
- **States:** Implement Skeleton (matching dimensions), EmptyState, and ErrorState for data-driven parts.
- **UX:** Avoid empty screens; always provide meaningful states.
- **Styling:** Use Tailwind CSS, `cn()`, and Design Tokens. No Hex/RGB or arbitrary values (use Tailwind scale strictly). When writing Tailwind CSS, always use v4 syntax and canonical classes (e.g., use `bg-linear-to-*` instead of `bg-gradient-to-*`, `size-*` instead of `w-* h-*`).
- **Primitive Components:** Build primitive UI components (Button, Dialog, etc.) in `src/components/ui`.
  - **CRITICAL CHECK:** Check `package.json` for UI primitives.
  - If `@radix-ui/*` is installed, use Radix UI primitives.
  - If `@base-ui/react` is installed, use Base UI primitives.
  - If neither is installed, build accessible native HTML elements.
- **A11y:** Enforce semantic HTML, `aria` attributes, and keyboard accessibility.
- **Typography:** One `h1` per page. Do not skip heading levels. Do not use heading styles for non-heading content.
- **Responsive:** Mobile-first from 320px. Use and verify at Tailwind defaults: sm 640, md 768, lg 1024, xl 1280.
- **Focus:** Overlays/dialogs must move focus inside on open and restore it on close. Prefer Radix/Base UI primitives for focus trap; never invent a custom trap when a primitive is available.
- **Exports:** Use Named exports only.

## Bans
- **AI Aesthetic:** Default purple/indigo palettes, excessive gradients, overuse of `rounded-2xl`, generic card/grid layouts.
- **Magic Numbers:** Arbitrary values (e.g., `p-[13px]`); use Tailwind’s spacing scale strictly.
- **UX/A11y:** `div` soup, "Lorem Ipsum", or `onClick` on non-interactive elements without keyboard support.
- **Logic:** Default exports or data fetching inside atomic components.

## Delivery Checklist
Before marking UI work done, confirm all of the following:
- Interactive elements are reachable and operable via keyboard (Tab / Enter / Space / Escape where applicable).
- Focus is visible; dialogs/sheets manage focus correctly.
- Loading, empty, and error states exist for data-driven UI.
- Spacing, color, and typography use design tokens / Tailwind scale (no raw hex or arbitrary values).
- Layout holds at 320px and Tailwind defaults: sm 640, md 768, lg 1024, xl 1280.
- No console errors or a11y anti-patterns introduced (`div` buttons, missing labels, color-only state).

## Mandatory Patterns

### 1. Tailwind Class Merging
NEVER use dynamic string interpolation (`${class1} ${class2}`) for Tailwind classes. ALWAYS use the `cn()` utility function.

<example>
import { cn } from "@/lib/utils";

export const Button = ({ className, variant }) => (
  <button className={cn("base-classes", variant === "primary" && "bg-blue-500", className)}>
    Click
  </button>
);
</example>

## Correct Implementation

<example>
```tsx
export const Card = ({ title, children }: { title: string; children: React.ReactNode }) => (
  <section className={cn("p-4 md:p-6", "bg-surface border rounded-md")}>
    <h3 className="text-base font-semibold">{title}</h3>
    <div className="mt-4">{children}</div>
  </section>
);
```
</example>

## Incorrect Implementation

<incorrect-example>
```tsx
export default function ActionButton({ label }: { label: string }) {
  return (
    <div 
      style={{ backgroundColor: '#6366f1' }}
      className="rounded-2xl p-[11px] shadow-2xl"
      onClick={() => console.log('clicked')}
    >
      <span>{label}</span>
    </div>
  );
}
```
</incorrect-example>
